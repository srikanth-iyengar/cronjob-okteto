name: Cron Job for jenkins server

on:
  schedule:
   - cron: "*/5 * * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
     - name: Not on Monday or Wednesday
       run: |
         echo "Hello world"
         echo "Hello world"
         echo "Hello world"
         echo "Hello world"
