version: "2.1"
jobs:
  build:
    docker:
      - image: ubuntu
    steps:
      - checkout
  test:
    docker:
      - image: ubuntu
    steps:
      - checkout
workflows:
  build_and_test:
    jobs:
      - build
      - test
